# Ports

obliterate processes that are hogging your ports

## Usage

You can build the binary with: `cargo build --release`

You can run the tool without the tui: `ports` or `ports -p [udp/tcp]` (default
is tcp)

You can run the TUI with: `ports tui` or `ports -p [udp/tcp] tui`

